//for each game:
//current tournaments --> results+matches in said tournament
//upcoming tournaments --> matches in said tournament
const rootUrl = "http://localhost:3000";

export async function getCODTournaments() {
  const data = await fetch;
}
